let x : int = y
let y : byte = 42

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="assign_from_byte",
      scope=Scope(
        bindings=[
          Binding(
            name="x",
            kind=Variable,
            visibility=Default,
            type=int,
            value=IdentifierExpression(
              name="y"
            )
          ),
          Binding(
            name="y",
            kind=Variable,
            visibility=Default,
            type=byte,
            value=NumberLiteralExpression(
              lit=42
            )
          )
        ]
      )
    )
  ]
)
*/
