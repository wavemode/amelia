let x : float = y
let y : float = 42

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="assign_from_float",
      scope=Scope(
        bindings=[
          Binding(
            name="x",
            kind=Variable,
            visibility=Default,
            type=float,
            value=IdentifierExpression(
              name="y"
            )
          ),
          Binding(
            name="y",
            kind=Variable,
            visibility=Default,
            type=float,
            value=NumberLiteralExpression(
              lit=42
            )
          )
        ]
      )
    )
  ]
)
*/
