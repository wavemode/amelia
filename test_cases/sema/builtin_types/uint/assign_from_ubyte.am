let x : uint = y
let y : ubyte = 42

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="assign_from_ubyte",
      scope=Scope(
        bindings=[
          Binding(
            name="x",
            kind=Variable,
            visibility=Default,
            type=uint,
            value=BuiltinTypeCastExpression(
              type=uint,
              expr=IdentifierExpression(
                name="y"
              )
            )
          ),
          Binding(
            name="y",
            kind=Variable,
            visibility=Default,
            type=ubyte,
            value=BuiltinTypeCastExpression(
              type=ubyte,
              expr=NumberLiteralExpression(
                lit=42
              )
            )
          )
        ]
      )
    )
  ]
)
*/
