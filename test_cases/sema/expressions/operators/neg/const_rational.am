const x = -5.0

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="const_rational",
      scope=Scope(
        bindings=[
          ValueBinding(
            name="x",
            type=Const[-5.0],
            value=NativeUnaryOperationExpression(
              operator="-",
              operand=NumberLiteralExpression(
                lit=5.0
              )
            )
          )
        ]
      )
    )
  ]
)
*/
