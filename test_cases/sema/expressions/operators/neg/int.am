let x : int = 1
let z : int = -x

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="int",
      scope=Scope(
        bindings=[
          ValueBinding(
            name="x",
            type=int,
            value=NumberLiteralExpression(
              lit=1
            )
          ),
          ValueBinding(
            name="z",
            type=int,
            value=NativeUnaryOperationExpression(
              op_kind="-",
              operand=IdentifierExpression(
                name="x"
              )
            )
          )
        ]
      )
    )
  ]
)
*/
