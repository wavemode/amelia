const x = true
const z : const false = !x

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      scope=Scope(
        bindings=[
          ValueBinding(
            name="x",
            type=Const[true],
            value=BooleanLiteralExpression(
              value=true
            )
          ),
          ValueBinding(
            name="z",
            type=Const[false],
            value=NativeUnaryOperationExpression(
              op_kind="!",
              operand=IdentifierExpression(
                name="x"
              )
            )
          )
        ]
      )
    )
  ]
)
*/
