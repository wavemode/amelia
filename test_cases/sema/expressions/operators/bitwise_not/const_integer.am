const x = ~5

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      scope=Scope(
        bindings=[
          ValueBinding(
            name="x",
            type=Const[-6],
            value=NativeUnaryOperationExpression(
              op_kind="~",
              operand=NumberLiteralExpression(
                lit=5
              )
            )
          )
        ]
      )
    )
  ]
)
*/
