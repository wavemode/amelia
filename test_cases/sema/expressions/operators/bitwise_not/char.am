let x : char = 'a'
let z : uint = ~x

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="char",
      scope=Scope(
        bindings=[
          ValueBinding(
            name="x",
            type=char,
            value=CharLiteralExpression['a']
          ),
          ValueBinding(
            name="z",
            type=uint,
            value=NativeUnaryOperationExpression(
              operator="~",
              operand=IdentifierExpression(
                name="x"
              )
            )
          )
        ]
      )
    )
  ]
)
*/
