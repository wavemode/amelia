let x : usize = 1
let z : usize = +x

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      scope=Scope(
        bindings=[
          ValueBinding(
            name="x",
            type=usize,
            value=NativeTypeCastExpression(
              type=usize,
              expr=NumberLiteralExpression(
                lit=1
              )
            )
          ),
          ValueBinding(
            name="z",
            type=usize,
            value=NativeUnaryOperationExpression(
              op_kind="+",
              operand=IdentifierExpression(
                name="x"
              )
            )
          )
        ]
      )
    )
  ]
)
*/
