let x : byte = 1
let z : byte = +x

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="byte",
      scope=Scope(
        bindings=[
          ValueBinding(
            name="x",
            type=byte,
            value=NativeTypeCastExpression(
              expr=NumberLiteralExpression(
                lit=1
              )
            )
          ),
          ValueBinding(
            name="z",
            type=byte,
            value=IdentifierExpression(
              name="x"
            )
          )
        ]
      )
    )
  ]
)
*/
