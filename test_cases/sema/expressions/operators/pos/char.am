let x : char = 'a'
let z : char = +x

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      scope=Scope(
        bindings=[
          ValueBinding(
            name="x",
            type=char,
            value=CharLiteralExpression['a']
          ),
          ValueBinding(
            name="z",
            type=char,
            value=NativeUnaryOperationExpression(
              op_kind="+",
              operand=IdentifierExpression(
                name="x"
              )
            )
          )
        ]
      )
    )
  ]
)
*/
