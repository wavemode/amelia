const x = 5
let y = &const x

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="const_to_const",
      scope=Scope(
        bindings=[
          Binding(
            name="x",
            type=Const[5],
            value=NumberLiteralExpression(
              lit=5
            ),
            kind=Constant,
            visibility=Default
          ),
          Binding(
            name="y",
            type=&const Const[5],
            value=AddressOfExpression(
              operand=IdentifierExpression(
                name="x"
              )
            ),
            kind=Variable,
            visibility=Default
          )
        ]
      )
    )
  ]
)
*/
