let x = 5
let y = &x

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="mut",
      scope=Scope(
        bindings=[
          ValueBinding(
            name="x",
            type=int,
            value=NumberLiteralExpression(
              lit=5
            )
          ),
          ValueBinding(
            name="y",
            type=&int,
            value=ReferenceExpression(
              referent=IdentifierExpression(
                name="x"
              ),
              is_const=false,
              is_move=false
            )
          )
        ]
      )
    )
  ]
)
*/
