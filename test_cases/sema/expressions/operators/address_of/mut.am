let x = 5
let y = &x

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="mut",
      scope=Scope(
        bindings=[
          Binding(
            name="x",
            type=int,
            value=NumberLiteralExpression(
              lit=5
            ),
            kind=Variable,
            visibility=Default
          ),
          Binding(
            name="y",
            type=&int,
            value=AddressOfExpression(
              operand=IdentifierExpression(
                name="x"
              )
            ),
            kind=Variable,
            visibility=Default
          )
        ]
      )
    )
  ]
)
*/
