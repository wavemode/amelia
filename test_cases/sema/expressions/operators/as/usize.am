let x = 50
let z = x as usize

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      scope=Scope(
        bindings=[
          ValueBinding(
            name="x",
            type=int,
            value=NumberLiteralExpression(
              lit=50
            )
          ),
          ValueBinding(
            name="z",
            type=usize,
            value=NativeTypeCastExpression(
              type=usize,
              expr=IdentifierExpression(
                name="x"
              )
            )
          )
        ]
      )
    )
  ]
)
*/
