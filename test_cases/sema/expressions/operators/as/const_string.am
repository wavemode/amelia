const x = "hello" as &str

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      scope=Scope(
        bindings=[
          ValueBinding(
            name="x",
            type=&str,
            value=StringLiteralExpression["hello"]
          )
        ]
      )
    )
  ]
)
*/
