let x : float = 1
let z = x as bitint[4]

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="bitint",
      scope=Scope(
        bindings=[
          ValueBinding(
            name="x",
            type=float,
            value=NativeTypeCastExpression(
              expr=NumberLiteralExpression(
                lit=1
              )
            )
          ),
          ValueBinding(
            name="z",
            type=bitint[4],
            value=NativeTypeCastExpression(
              expr=IdentifierExpression(
                name="x"
              )
            )
          )
        ]
      )
    )
  ]
)
*/
