let x = 50
let z = x as ushort

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      scope=Scope(
        bindings=[
          ValueBinding(
            name="x",
            type=int,
            value=NumberLiteralExpression(
              lit=50
            )
          ),
          ValueBinding(
            name="z",
            type=ushort,
            value=NativeTypeCastExpression(
              type=ushort,
              expr=IdentifierExpression(
                name="x"
              )
            )
          )
        ]
      )
    )
  ]
)
*/
