let x = {
  let y = 4
}

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="nothing_after_let",
      scope=Scope(
        bindings=[
          Binding(
            name="x",
            type=null,
            value=ValueBindingExpression(
              name="y",
              type=null,
              value=NumberLiteralExpression(
                lit=4
              )
            ),
            kind=Variable,
            visibility=Default
          )
        ]
      )
    )
  ]
)
*/
