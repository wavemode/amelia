type A = int
let x = A

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      scope=Scope(
        bindings=[
          TypeBinding(
            name="A",
            type=alias::A
          ),
          ValueBinding(
            name="x",
            type=Type[alias::A],
            value=IdentifierExpression(
              name="A"
            )
          )
        ]
      )
    )
  ]
)
*/
