let x = 5.6

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="decimal",
      scope=Scope(
        bindings=[
          Binding(
            name="x",
            kind=Variable,
            visibility=Default,
            type=double,
            value=NumberLiteralExpression(
              5.6
            )
          )
        ]
      )
    )
  ]
)
*/
