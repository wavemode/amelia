let x = 5.6

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="decimal",
      scope=Scope(
        bindings=[
          Binding(
            name="x",
            type=Const[28/5],
            value=NumberLiteralExpression(
              lit=5.6
            ),
            kind=Variable,
            visibility=Default
          )
        ]
      )
    )
  ]
)
*/
