let x : const 5.0 = y
let y = 5

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="const_rational_assign_compatible",
      scope=Scope(
        bindings=[
          Binding(
            name="x",
            kind=Variable,
            visibility=Default,
            type=Const[5/1],
            value=IdentifierExpression(
              name="y"
            )
          ),
          Binding(
            name="y",
            kind=Variable,
            visibility=Default,
            type=Const[5],
            value=NumberLiteralExpression(
              lit=5
            )
          )
        ]
      )
    )
  ]
)
*/
