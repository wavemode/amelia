let x : const true = y
let y = true

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="const_boolean_assign_same",
      scope=Scope(
        bindings=[
          Binding(
            name="x",
            kind=Variable,
            visibility=Default,
            type=Const[true],
            value=IdentifierExpression(
              name="y"
            )
          ),
          Binding(
            name="y",
            kind=Variable,
            visibility=Default,
            type=Const[true],
            value=BooleanLiteralExpression(
              value=true
            )
          )
        ]
      )
    )
  ]
)
*/
