let x = 5

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="let_binding",
      scope=Scope(
        bindings=[
          Binding(
            name="x",
            kind=Variable,
            visibility=Default,
            type=Const[5],
            value=NumberLiteralExpression(
              lit=5
            )
          )
        ]
      )
    )
  ]
)
*/
