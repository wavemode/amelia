let x = 5

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="let_binding",
      scope=Scope(
        bindings=[
          Binding(
            name="x",
            type=Const[5],
            value=NumberLiteralExpression(
              lit=5
            ),
            kind=Variable,
            visibility=Default
          )
        ]
      )
    )
  ]
)
*/
