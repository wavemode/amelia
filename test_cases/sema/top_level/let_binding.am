const x = 5

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      scope=Scope(
        bindings=[
          ValueBinding(
            name="x",
            type=Const[5],
            value=NumberLiteralExpression(
              lit=5
            )
          )
        ]
      )
    )
  ]
)
*/
