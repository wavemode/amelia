const x = 5

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="const_binding",
      scope=Scope(
        bindings=[
          Binding(
            name="x",
            kind=Constant,
            visibility=Default,
            type=Const[5],
            value=NumberLiteralExpression(
              lit=5
            )
          )
        ]
      )
    )
  ]
)
*/
