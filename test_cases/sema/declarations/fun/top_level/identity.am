fun f(x : int) -> int {
  return x
}

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="identity",
      scope=Scope(
        bindings=[
          Binding(
            name="f",
            type=FunctionType(
              signatures=[
                Signature(
                  parameters=[
                    Parameter(
                      name="x",
                      type=int
                    )
                  ],
                  return_type=int
                )
              ]
            ),
            value=SequenceExpression(
              exprs=[
                ReturnExpression(
                  value=IdentifierExpression(
                    name="x"
                  )
                )
              ]
            ),
            kind=Function,
            visibility=Default
          )
        ]
      )
    )
  ]
)
*/
