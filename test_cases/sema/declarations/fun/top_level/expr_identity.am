fun f(x : int) -> int = x

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="expr_identity",
      scope=Scope(
        bindings=[
          ValueBinding(
            name="f",
            type=FunctionType(
              signatures=[
                Signature(
                  parameters=[
                    Parameter(
                      name="x",
                      type=int
                    )
                  ],
                  return_type=int
                )
              ]
            ),
            value=IdentifierExpression(
              name="x"
            )
          )
        ]
      )
    )
  ]
)
*/
