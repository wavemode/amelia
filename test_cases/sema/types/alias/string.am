type MyString = str

const x : &MyString = y
const y = "hello"

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      scope=Scope(
        bindings=[
          TypeBinding(
            name="MyString",
            type=string::MyString (aka str)
          ),
          ValueBinding(
            name="x",
            type=&string::MyString (aka str),
            value=IdentifierExpression(
              name="y"
            )
          ),
          ValueBinding(
            name="y",
            type=Const["hello"],
            value=StringLiteralExpression["hello"]
          )
        ]
      )
    )
  ]
)
*/
