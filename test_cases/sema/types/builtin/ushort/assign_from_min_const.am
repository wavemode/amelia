const x : ushort = 0

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="assign_from_min_const",
      scope=Scope(
        bindings=[
          Binding(
            name="x",
            type=ushort,
            value=BuiltinTypeCastExpression(
              expr=NumberLiteralExpression(
                lit=0
              )
            ),
            kind=Constant,
            visibility=Default
          )
        ]
      )
    )
  ]
)
*/
