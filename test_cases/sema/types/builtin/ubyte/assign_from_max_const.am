const x : ubyte = 255

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="assign_from_max_const",
      scope=Scope(
        bindings=[
          Binding(
            name="x",
            type=ubyte,
            value=BuiltinTypeCastExpression(
              expr=NumberLiteralExpression(
                lit=255
              )
            ),
            kind=Constant,
            visibility=Default
          )
        ]
      )
    )
  ]
)
*/
