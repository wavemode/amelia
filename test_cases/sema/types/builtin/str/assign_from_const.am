let x : &str = y
const y = "hello, world!"

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="assign_from_const",
      scope=Scope(
        bindings=[
          Binding(
            name="x",
            type=&str,
            value=BuiltinTypeCastExpression(
              expr=IdentifierExpression(
                name="y"
              )
            ),
            kind=Variable,
            visibility=Default
          ),
          Binding(
            name="y",
            type=Const["hello, world!"],
            value=StringLiteralExpression["hello, world!"],
            kind=Constant,
            visibility=Default
          )
        ]
      )
    )
  ]
)
*/
