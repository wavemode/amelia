const x : const 'a' = y
const y = 'a'

/* EXPECTED_OUTPUT:
SemaResult(
  modules=[
    Module(
      name="const_char_assign_same",
      scope=Scope(
        bindings=[
          Binding(
            name="x",
            type=Const['a'],
            value=IdentifierExpression(
              name="y"
            ),
            kind=Constant,
            visibility=Default
          ),
          Binding(
            name="y",
            type=Const['a'],
            value=CharLiteralExpression['a'],
            kind=Constant,
            visibility=Default
          )
        ]
      )
    )
  ]
)
*/
