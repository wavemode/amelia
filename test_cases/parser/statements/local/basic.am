local const x = 10

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    VisibilityNode(
      visibility=local,
      decl=ConstDeclarationNode(
        target=IdentifierNode(
          token=IDENTIFIER(x)
        ),
        expression=NumberLiteralNode(
          lit=NUMBER(10)
        )
      )
    )
  ]
)
*/
