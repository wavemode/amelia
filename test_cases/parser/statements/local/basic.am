local const x = 10

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    VisibilityNode(
      visibility=local,
      decl=ConstDeclarationNode(
        target=IdentifierNode(
          token=IDENTIFIER(x)
        ),
        expr=NumberLiteralNode(
          lit=NUMBER(10)
        )
      )
    )
  ]
)
*/
