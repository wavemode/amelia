const x = y

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    ConstStatementNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      expression=IdentifierNode(
        token=IDENTIFIER(y)
      )
    )
  ]
)
*/
