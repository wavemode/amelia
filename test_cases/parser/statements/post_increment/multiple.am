let example = {
  x++
  y++
}

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(example)
      ),
      expr=BlockExprNode(
        stmts=[
          PostIncrementStmtNode(
            target=IdentifierNode(
              token=IDENTIFIER(x)
            )
          ),
          PostIncrementStmtNode(
            target=IdentifierNode(
              token=IDENTIFIER(y)
            )
          )
        ]
      )
    )
  ]
)
*/
