let example = {
  return
  x()
}

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(example)
      ),
      expr=BlockExprNode(
        stmts=[
          ReturnStmtNode(),
          ExprStmtNode(
            expr=FunctionCallExprNode(
              callee=IdentifierNode(
                token=IDENTIFIER(x)
              )
            )
          )
        ]
      )
    )
  ]
)
*/
