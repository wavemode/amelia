let example = {
  --x
}

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(example)
      ),
      expr=BlockExprNode(
        stmts=[
          PreDecrementStmtNode(
            target=IdentifierNode(
              token=IDENTIFIER_NO_W(x)
            )
          )
        ]
      )
    )
  ]
)
*/
