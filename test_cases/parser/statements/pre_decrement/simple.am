let example = {
  --x
}

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclarationNode(
      target=IdentifierNode(
        token=IDENTIFIER(example)
      ),
      expr=BlockExpressionNode(
        stmts=[
          PreDecrementStatementNode(
            target=IdentifierNode(
              token=IDENTIFIER_NO_W(x)
            )
          )
        ]
      )
    )
  ]
)
*/
