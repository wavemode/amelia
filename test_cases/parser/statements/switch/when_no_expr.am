let example = {
  switch(y) {
    case (1) ++y
    case when (y < 10 && y > 3) --y
  }
}

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(example)
      ),
      expr=BlockExprNode(
        stmts=[
          SwitchStmtNode(
            expr=IdentifierNode(
              token=IDENTIFIER_NO_W(y)
            ),
            clauses=[
              CaseClauseNode(
                header=CaseClauseHeaderNode(
                  exprs=[
                    NumberLiteralNode(
                      lit=NUMBER(1)
                    )
                  ]
                ),
                body=PreIncrementStmtNode(
                  target=IdentifierNode(
                    token=IDENTIFIER_NO_W(y)
                  )
                )
              ),
              CaseClauseNode(
                header=CaseClauseHeaderNode(
                  when_clause=WhenClauseNode(
                    condition=AndExprNode(
                      left=LessExprNode(
                        left=IdentifierNode(
                          token=IDENTIFIER_NO_W(y)
                        ),
                        right=NumberLiteralNode(
                          lit=NUMBER(10)
                        )
                      ),
                      right=GreaterExprNode(
                        left=IdentifierNode(
                          token=IDENTIFIER(y)
                        ),
                        right=NumberLiteralNode(
                          lit=NUMBER(3)
                        )
                      )
                    )
                  )
                ),
                body=PreDecrementStmtNode(
                  target=IdentifierNode(
                    token=IDENTIFIER_NO_W(y)
                  )
                )
              )
            ]
          )
        ]
      )
    )
  ]
)
*/
