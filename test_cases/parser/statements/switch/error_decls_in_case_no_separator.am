switch(w) {
  case (
    let z = 10; const y = x + z  y
  ) ++x
  case (3) ++y
}

/* EXPECTED_OUTPUT:
ERROR("Multiple statements on the same line must be separated by a semicolon (at test_cases/parser/statements/switch/error_decls_in_case_no_separator.am:3:34)")
*/
