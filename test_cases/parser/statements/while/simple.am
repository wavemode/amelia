let example = {
  while (x) z
}

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(example)
      ),
      expr=BlockExprNode(
        stmts=[
          WhileStmtNode(
            condition=IdentifierNode(
              token=IDENTIFIER_NO_W(x)
            ),
            body=ExprStmtNode(
              expr=IdentifierNode(
                token=IDENTIFIER(z)
              )
            )
          )
        ]
      )
    )
  ]
)
*/
