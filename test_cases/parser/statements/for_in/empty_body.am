for (x in y);

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    ForInStatementNode(
      vars=[
        IdentifierNode(
          token=IDENTIFIER_NO_W(x)
        )
      ],
      iterable=IdentifierNode(
        token=IDENTIFIER(y)
      ),
      body=EmptyStatementNode()
    )
  ]
)
*/
