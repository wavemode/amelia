for (x in y);

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    ForInStatementNode(
      var=IdentifierNode(
        token=IDENTIFIER_NO_W(x)
      ),
      iterable=IdentifierNode(
        token=IDENTIFIER(y)
      ),
      body=EmptyStatementNode()
    )
  ]
)
*/
