let z = {
  for (x : &int, y : &String in z) w();
}

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(z)
      ),
      expr=BlockExprNode(
        stmts=[
          ForInStmtNode(
            vars=[
              ForInVariableNode(
                name=IdentifierNode(
                  token=IDENTIFIER_NO_W(x)
                ),
                type=RefExprNode(
                  is_const=false,
                  is_move=false,
                  expr=PrimitiveTypeNode(
                    token=KEYWORD_INT(int)
                  )
                )
              ),
              ForInVariableNode(
                name=IdentifierNode(
                  token=IDENTIFIER(y)
                ),
                type=RefExprNode(
                  is_const=false,
                  is_move=false,
                  expr=IdentifierNode(
                    token=IDENTIFIER_NO_W(String)
                  )
                )
              )
            ],
            iterable=IdentifierNode(
              token=IDENTIFIER(z)
            ),
            body=ExprStmtNode(
              expr=FunctionCallExprNode(
                callee=IdentifierNode(
                  token=IDENTIFIER(w)
                )
              )
            )
          ),
          EmptyStmtNode()
        ]
      )
    )
  ]
)
*/
