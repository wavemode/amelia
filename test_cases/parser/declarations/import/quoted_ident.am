import `abc`

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    ImportDeclNode(
      path=IdentifierNode(
        token=QUOTED_IDENTIFIER(`abc`)
      )
    )
  ],
  imports=[
    ImportDeclNode(
      path=IdentifierNode(
        token=QUOTED_IDENTIFIER(`abc`)
      )
    )
  ]
)
*/
