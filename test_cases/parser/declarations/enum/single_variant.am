enum A {
  FirstOption,
}

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    EnumDeclNode(
      name=IdentifierNode(
        token=IDENTIFIER(A)
      ),
      variants=[
        EnumVariantNode(
          name=IdentifierNode(
            token=IDENTIFIER(FirstOption)
          )
        )
      ]
    )
  ]
)
*/
