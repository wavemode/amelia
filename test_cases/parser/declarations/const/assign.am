const x = y

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    ConstDeclarationNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      expr=IdentifierNode(
        token=IDENTIFIER(y)
      )
    )
  ]
)
*/
