const x = y

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    ConstDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      expr=IdentifierNode(
        token=IDENTIFIER(y)
      )
    )
  ]
)
*/
