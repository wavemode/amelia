let x = await y

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetStatementNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      expression=AwaitExpressionNode(
        expr=IdentifierNode(
          token=IDENTIFIER(y)
        )
      )
    )
  ]
)
*/
