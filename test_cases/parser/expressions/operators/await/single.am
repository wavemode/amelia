let x = await y

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetAssignmentStatementNode(
      target=IdentifierNode(
        name=IDENTIFIER(x)
      ),
      expression=AwaitExpressionNode(
        expr=IdentifierNode(
          name=IDENTIFIER(y)
        )
      )
    )
  ]
)
*/
