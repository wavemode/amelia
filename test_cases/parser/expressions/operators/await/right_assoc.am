let w = await await x

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetAssignmentStatementNode(
      target=IdentifierNode(
        token=IDENTIFIER(w)
      ),
      expression=AwaitExpressionNode(
        expr=AwaitExpressionNode(
          expr=IdentifierNode(
            token=IDENTIFIER(x)
          )
        )
      )
    )
  ]
)
*/
