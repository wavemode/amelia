let w = await await x

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        name="w"
      ),
      expr=AwaitExprNode(
        expr=AwaitExprNode(
          expr=IdentifierNode(
            name="x"
          )
        )
      )
    )
  ]
)
*/
