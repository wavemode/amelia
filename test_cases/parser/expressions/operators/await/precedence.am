let x = await y * z

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      expr=MultiplyExprNode(
        left=AwaitExprNode(
          expr=IdentifierNode(
            token=IDENTIFIER(y)
          )
        ),
        right=IdentifierNode(
          token=IDENTIFIER(z)
        )
      )
    )
  ]
)
*/
