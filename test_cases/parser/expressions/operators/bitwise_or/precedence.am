let w = x && y | z

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(w)
      ),
      expr=AndExprNode(
        left=IdentifierNode(
          token=IDENTIFIER(x)
        ),
        right=BitwiseOrExprNode(
          left=IdentifierNode(
            token=IDENTIFIER(y)
          ),
          right=IdentifierNode(
            token=IDENTIFIER(z)
          )
        )
      )
    )
  ]
)
*/
