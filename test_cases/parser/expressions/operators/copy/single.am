let x = copy y

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclarationNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      expr=CopyExpressionNode(
        expr=IdentifierNode(
          token=IDENTIFIER(y)
        )
      )
    )
  ]
)
*/
