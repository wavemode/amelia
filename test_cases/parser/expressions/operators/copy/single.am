let x = copy y

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      expr=CopyExprNode(
        expr=IdentifierNode(
          token=IDENTIFIER(y)
        )
      )
    )
  ]
)
*/
