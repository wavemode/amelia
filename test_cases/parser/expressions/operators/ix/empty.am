let x = y[]

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      expr=IndexingExprNode(
        object=IdentifierNode(
          token=IDENTIFIER(y)
        )
      )
    )
  ]
)
*/
