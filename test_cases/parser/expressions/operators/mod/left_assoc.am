let w = x % y % z

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(w)
      ),
      expr=ModuloExprNode(
        left=ModuloExprNode(
          left=IdentifierNode(
            token=IDENTIFIER(x)
          ),
          right=IdentifierNode(
            token=IDENTIFIER(y)
          )
        ),
        right=IdentifierNode(
          token=IDENTIFIER(z)
        )
      )
    )
  ]
)
*/
