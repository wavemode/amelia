let x = -y

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      expr=NegativeExprNode(
        expr=IdentifierNode(
          token=IDENTIFIER_NO_W(y)
        )
      )
    )
  ]
)
*/
