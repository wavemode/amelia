let x = + +y

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclarationNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      expr=PositiveExpressionNode(
        expr=PositiveExpressionNode(
          expr=IdentifierNode(
            token=IDENTIFIER_NO_W(y)
          )
        )
      )
    )
  ]
)
*/
