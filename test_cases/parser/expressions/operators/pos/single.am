let x = +y

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      expr=PositiveExprNode(
        expr=IdentifierNode(
          token=IDENTIFIER_NO_W(y)
        )
      )
    )
  ]
)
*/
