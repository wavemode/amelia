let x : *const T

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      type=DerefExprNode(
        is_const=true,
        expr=IdentifierNode(
          token=IDENTIFIER(T)
        )
      )
    )
  ]
)
*/
