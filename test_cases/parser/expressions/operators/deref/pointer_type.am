let x : *T

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      type=DerefExprNode(
        is_const=false,
        expr=IdentifierNode(
          token=IDENTIFIER_NO_W(T)
        )
      )
    )
  ]
)
*/
