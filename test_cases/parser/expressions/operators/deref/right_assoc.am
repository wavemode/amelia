let x = **y

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclarationNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      expr=DerefExpressionNode(
        is_const=false,
        expr=DerefExpressionNode(
          is_const=false,
          expr=IdentifierNode(
            token=IDENTIFIER_NO_W(y)
          )
        )
      )
    )
  ]
)
*/
