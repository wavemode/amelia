let x = *y

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      expr=DerefExprNode(
        is_const=false,
        expr=IdentifierNode(
          token=IDENTIFIER_NO_W(y)
        )
      )
    )
  ]
)
*/
