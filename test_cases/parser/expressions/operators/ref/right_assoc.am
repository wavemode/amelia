let w = & & x

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclarationNode(
      target=IdentifierNode(
        token=IDENTIFIER(w)
      ),
      expression=RefExpressionNode(
        is_const=false,
        is_move=false,
        expr=RefExpressionNode(
          is_const=false,
          is_move=false,
          expr=IdentifierNode(
            token=IDENTIFIER(x)
          )
        )
      )
    )
  ]
)
*/
