let w = & & x

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetAssignmentStatementNode(
      target=IdentifierNode(
        token=IDENTIFIER(w)
      ),
      expression=RefExpressionNode(
        is_const=false,
        expr=RefExpressionNode(
          is_const=false,
          expr=IdentifierNode(
            token=IDENTIFIER(x)
          )
        )
      )
    )
  ]
)
*/
