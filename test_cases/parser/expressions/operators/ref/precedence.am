let x = & y * z

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      expr=MultiplyExprNode(
        left=RefExprNode(
          is_const=false,
          is_move=false,
          expr=IdentifierNode(
            token=IDENTIFIER(y)
          )
        ),
        right=IdentifierNode(
          token=IDENTIFIER(z)
        )
      )
    )
  ]
)
*/
