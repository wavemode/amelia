let x : &T[U]

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      type=RefExprNode(
        is_const=false,
        is_move=false,
        expr=IndexingExprNode(
          object=IdentifierNode(
            token=IDENTIFIER_NO_W(T)
          ),
          indices=[
            IndexNode(
              value=IdentifierNode(
                token=IDENTIFIER_NO_W(U)
              )
            )
          ]
        )
      )
    )
  ]
)
*/
