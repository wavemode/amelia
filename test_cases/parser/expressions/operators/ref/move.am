let x : &move int

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      type=RefExprNode(
        is_const=false,
        is_move=true,
        expr=PrimitiveTypeNode(
          token=KEYWORD_INT(int)
        )
      )
    )
  ]
)
*/
