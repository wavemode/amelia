let x : &const int

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      type=RefExprNode(
        is_const=true,
        is_move=false,
        expr=PrimitiveTypeNode(
          token=KEYWORD_INT(int)
        )
      )
    )
  ]
)
*/
