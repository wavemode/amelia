let x = move y

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclarationNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      expr=MoveExpressionNode(
        expr=IdentifierNode(
          token=IDENTIFIER(y)
        )
      )
    )
  ]
)
*/
