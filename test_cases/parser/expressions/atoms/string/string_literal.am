let x = "hello, world!"

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      expr=StringLiteralNode(
        lit="hello, world!"
      )
    )
  ]
)
*/
