let x = { .y = z, .a = b, }

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetStatementNode(
      target=IdentifierNode(
        name=IDENTIFIER(x)
      ),
      expression=ObjectLiteralNode(
        entries=[
          DOTTED_IDENTIFIER(.y) = IdentifierNode(
            name=IDENTIFIER(z)
          ),
          DOTTED_IDENTIFIER(.a) = IdentifierNode(
            name=IDENTIFIER(b)
          )
        ]
      )
    )
  ]
)
*/
