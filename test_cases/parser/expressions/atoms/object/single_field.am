let x = { .y = z }

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclarationNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      expression=ObjectLiteralNode(
        entries=[
          KeyValueEntryNode(
            key=IdentifierNode(
              token=IDENTIFIER_NO_W(y)
            ),
            value=IdentifierNode(
              token=IDENTIFIER(z)
            )
          )
        ]
      )
    )
  ]
)
*/
