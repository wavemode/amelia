let x = { .y = z }

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetStatementNode(
      target=IdentifierNode(
        name=IDENTIFIER(x)
      ),
      expression=ObjectLiteralNode(
        entries=[
          DOTTED_IDENTIFIER(.y) = IdentifierNode(
            name=IDENTIFIER(z)
          )
        ]
      )
    )
  ]
)
*/
