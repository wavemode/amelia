let w = try x catch (y : z) t

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetAssignmentStatementNode(
      target=IdentifierNode(
        token=IDENTIFIER(w)
      ),
      expression=TryExpressionNode(
        try_block=IdentifierNode(
          token=IDENTIFIER(x)
        ),
        clauses=[
          CatchClauseBindingNode(
            var=IdentifierNode(
              token=IDENTIFIER_NO_W(y)
            ),
            exc_type=IdentifierNode(
              token=IDENTIFIER(z)
            ),
            body=IdentifierNode(
              token=IDENTIFIER(t)
            )
          )
        ]
      )
    )
  ]
)
*/
