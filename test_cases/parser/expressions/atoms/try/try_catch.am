let w = try x catch (y : z) t

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclarationNode(
      target=IdentifierNode(
        token=IDENTIFIER(w)
      ),
      expr=TryExpressionNode(
        try_block=IdentifierNode(
          token=IDENTIFIER(x)
        ),
        catch_clauses=[
          CatchClauseNode(
            exc_type=IdentifierNode(
              token=IDENTIFIER(z)
            ),
            var=IdentifierNode(
              token=IDENTIFIER_NO_W(y)
            ),
            body=IdentifierNode(
              token=IDENTIFIER(t)
            )
          )
        ]
      )
    )
  ]
)
*/
