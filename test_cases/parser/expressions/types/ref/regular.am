let x : &int

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclarationNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      type=RefExpressionNode(
        is_const=false,
        is_move=false,
        expr=PrimitiveTypeNode(
          token=KEYWORD_INT(int)
        )
      )
    )
  ]
)
*/
