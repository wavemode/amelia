let x : &const [T]

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclarationNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      type=RefExpressionNode(
        is_const=true,
        is_move=false,
        expr=BracketExpressionNode(
          exprs=[
            IdentifierNode(
              token=IDENTIFIER_NO_W(T)
            )
          ]
        )
      )
    )
  ]
)
*/
