let x : &[T]

/* EXPECTED_OUTPUT:
ModuleNode(
  decls=[
    LetDeclNode(
      target=IdentifierNode(
        token=IDENTIFIER(x)
      ),
      type=RefExprNode(
        is_const=false,
        is_move=false,
        expr=BracketExprNode(
          exprs=[
            IdentifierNode(
              token=IDENTIFIER_NO_W(T)
            )
          ]
        )
      )
    )
  ]
)
*/
